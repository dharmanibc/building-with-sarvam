# Copy this file to Labs/.env and fill in your key.
# Free key + Rs 1000 credit: https://indus.sarvam.ai/
SARVAM_API_KEY=sk_your_key_here

# Optional. If your key already lives in an env file somewhere else, point at it
# instead of creating a .env — every notebook honours this variable:
#   export SARVAM_ENV_FILE=/path/to/your/existing.env
